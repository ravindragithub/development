({
    showAccountSection : function(component, event, helper){
        var customerAccountId = component.get("v.childWrap.customerAccount.Id");
        if(customerAccountId != component.get("v.displayDetail"))	
        component.set("v.displayDetail",customerAccountId);
        else
        component.set("v.displayDetail",'');    
    },
})