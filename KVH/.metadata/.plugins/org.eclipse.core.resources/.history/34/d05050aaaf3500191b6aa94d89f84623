({
    selectRatePlan : function(component, event, helper) {
        var changeValue = event.getSource().get("v.value");
        component.set("v.simpleLTEActivation.Rate_Plan__c",changeValue);
    }
})