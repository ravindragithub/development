trigger PortTrigger on Port__c (after insert, after update, before delete, before insert, before update) {
	// This is the only line of code that is required.
	TriggerFactory.createTriggerDispatcher(Port__c.sObjectType);
}